'use strict';

angular
  .module('happyWorkspace.controllers')
  .controller('RHCtrl', RHCtrl);

RHCtrl.$inject = ['$scope','$rootScope','$location','$timeout','$window','$http','env'];

function RHCtrl($scope,$rootScope,$location,$timeout,$window,$http,env){

  /**-------------------------------
         View Model Definition
  ---------------------------------**/

  var vm = this;

  
}